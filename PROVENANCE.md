# Provenance — hestiaos-templates

```
Origin       : fresh-root clean-room repository. No legacy git history.
Content       : templates only (skeletons + document templates). No product source.
Source-copy   : none (templates are authored placeholders, not extracted code).
Images/ISO    : none.
Secrets       : none.
Canonical home: <projects-root>/hestiaos-templates (after push).
Remote truth  : hestiaos/hestiaos-templates (when pushed via push-gate).
```
